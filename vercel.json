{
    "buildCommand": "cd frontend && npm install && chmod +x node_modules/.bin/react-scripts && PUBLIC_URL=/ CI=false npm run build",
    "outputDirectory": "frontend/build",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}